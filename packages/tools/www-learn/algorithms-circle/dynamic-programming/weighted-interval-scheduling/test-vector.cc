#include <vector>

using std::vector;

int main() {
  vector<int> myVector;
  myVector.push_back(12);
  myVector.push_back(34);
  myVector.push_back(56);
  
  //vector<int> newVector = myVector + 78; // illegal
  return 0;
}
