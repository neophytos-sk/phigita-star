from the discussion forums
problem: ObserveEvidence incorrect even though everything else correct?
solution:
https://class.coursera.org/pgm-003/forum/thread?thread_id=212#post-981
I think it is the Standardize call in the submit.m file.
Please, try this.
Edit submit.m, go to line 315, that looks like:

F = StandardizeFactors(F);

and comment it out:

%F = StandardizeFactors(F);

Save the file, and try to submit again.
