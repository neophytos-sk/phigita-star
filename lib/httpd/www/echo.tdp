contract {
    param -id msg -default "" -optional_p 1
    # param -id msg2 -default "" -optional_p 1
}

master -src templates/blank-master.inc {
    ul {
        li {
            t "hello, msg=@{msg}"
        }
        # li { t "msg2=@{msg2}" }
    }
}
